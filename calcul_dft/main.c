#include <stdio.h>
#include <stdlib.h>

void calcul(int * resIm, int * resRe,int * module, int k);
int * resRe;
int * resIm;
int * module;

int main(){
		res = malloc(sizeof(int));
		calcul(res, 1);
		while(1){}
}
